# Runtime + hub-callable dependencies for ovnis-pr. federation.json's
# `setup` command and the CI workflows install from this file.
pytest
jsonschema
# Shared federation packages from thehub-pr, resolved via a local editable path
# instead of a pinned commit SHA: edit the package once in thehub-pr/packages
# and every consumer picks it up with no per-repo bump. Requires thehub-pr to be
# checked out as a sibling of this repo (the paths below are relative to the repo
# root, where `pip install -r requirements.txt` runs).
-e ../thehub-pr/packages/prii_maintenance
-e ../thehub-pr/packages/prii_export_utils
